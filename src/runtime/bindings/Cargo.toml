[package]
name = "arc_bindings"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
arc_shared = { path = "../shared" }
syn = { version = "~2.0", features = ["full"] }
quote = "~1.0"
